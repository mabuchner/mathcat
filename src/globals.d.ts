/** Build timestamp (ISO 8601), injected by Vite's `define` in vite.config.ts. */
declare const __BUILD_DATE__: string

/** Short commit hash of the built revision, injected by Vite's `define` in vite.config.ts. */
declare const __COMMIT_HASH__: string
